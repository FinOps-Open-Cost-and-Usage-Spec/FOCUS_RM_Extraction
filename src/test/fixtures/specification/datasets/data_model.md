# Focus Data Model

The root entity, required so the extractor has a DataModel to expand.

## Requirements

FocusDataModel MUST adhere to the following requirements:

* FocusDataModel MUST include BillingPeriod.

## Data Model ID

FocusDataModel

## Display Name

Focus Data Model
